from strands import Agent

# Create an agent with default settings
agent = Agent()

# Ask the agent a question
response = agent("como faço para pegar e guarda informaçoes sobre as mares e praias para que eu possa usa-las em conjunto com strands agent")
print(response)